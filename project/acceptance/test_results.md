# Test Results Report

**TDD data deliverable D3** · Agreement No. 5600002690012
River Hawk Consulting, LLC · UNCLASSIFIED

Generated by `scripts/make-test-report.js` directly from the Playwright JSON reporter
output in `project/evidence/`. The numbers below are read from the run, not transcribed,
so this document cannot claim a result the suite did not produce.

| Suite | Specs | Passed | Failed | Flaky | Duration | Run at | Verdict |
|---|---|---|---|---|---|---|---|
| Angular build (delivered) | 68 | 68 | 0 | 0 | 8.1 s | 2026-08-08 12:33:57Z | **PASS** |
| React reference (evaluated Phase 2 build) | 16 | 16 | 0 | 0 | 6.2 s | 2026-08-08 10:19:34Z | **PASS** |

Evidence files: `project/evidence/e2e-results-2026-08-08.json`, `project/evidence/react-reference-results-2026-08-08.json`.

## Angular build — specs by file

### `ng/bilingual.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | threads open bilingual: English leads, source always on screen, no toggle | 450 ms |
| ✅ | correction happens against the source; review state is obvious | 412 ms |

### `ng/bulk-confirm.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the bulk accept is not offered when reading each message is the job | 431 ms |
| ✅ | a long thread can be cleared in one action, after stating the count | 596 ms |
| ✅ | declining the accept changes nothing | 491 ms |
| ✅ | the gold copy reports bulk acceptance as what it was | 655 ms |

### `ng/conversation-note.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | a conversation note is recorded against the thread and shown above the stream | 544 ms |
| ✅ | the conversation note leads the gold copy, ahead of the transcript | 579 ms |
| ✅ | a conversation note can be withdrawn | 327 ms |
| ✅ | the note follows the conversation, not the screen | 395 ms |

### `ng/customs-file.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | the customs file opens, pages through all five, and reads RTL | 582 ms |
| ✅ | the customs file carries an English gloss covering the whole document | 312 ms |
| ✅ | the customs file is reachable by its declaration number and its entities | 486 ms |

### `ng/enrichment.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | auto-translation renders and thread-level entity extraction chips every message | 621 ms |
| ✅ | translation review and analyst note (smoke-6 review flow, clips removed) | 457 ms |
| ✅ | the one output: export is locked until a thread is promoted, then carries verdicts and notes | 550 ms |

### `ng/export-modal-fit.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | the transcript field stays inside the export dialog, restored and maximized | 755 ms |
| ✅ | maximize grows the dialog in both directions, not just sideways | 559 ms |

### `ng/flagged-facet.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | flagging surfaces a Flagged facet that isolates the handoff pile | 706 ms |
| ✅ | flagged-first sorting lifts the handoff pile without losing the rest | 271 ms |
| ✅ | the flagged filter clears with everything else | 441 ms |

### `ng/gold-copy-panel.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | copying the export confirms it happened | 767 ms |
| ✅ | the export dialog is large and maximizes | 740 ms |
| ✅ | gold threads reorder by drag, and the export follows the new order | 1150 ms |
| ✅ | both side panels resize by dragging, and double-click restores | 404 ms |

### `ng/multipage-doc.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | multi-page Russian document: pages, Cyrillic blocks, and gloss | 417 ms |
| ✅ | the contract surfaces in a flagged-numbers watchlist sweep | 486 ms |

### `ng/ocr-bilingual-lines.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | an RTL document puts the original on the right and the English on the left | 384 ms |
| ✅ | an LTR document puts the original on the left and the English on the right | 344 ms |
| ✅ | every line on the page is paired, and the pairing follows paging | 389 ms |

### `ng/ocr-gloss-review.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the document translation can be confirmed, and the verdict taken back | 419 ms |
| ✅ | the document translation can be corrected against the original | 426 ms |
| ✅ | the document can be sent back for a fresh translation | 472 ms |
| ✅ | the scan and the two text layers stack in reading order | 357 ms |

### `ng/ocr-threadgold.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | officer document annotation: tag + note, and the tag filters the queue | 483 ms |
| ✅ | OCR viewer: extracted blocks render with engine caption | 291 ms |
| ✅ | thread gold: translate thread, review all, promote — the stack ticks | 440 ms |

### `ng/ocr-viewer-size.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the scan viewer maximizes and the image grows with it | 473 ms |
| ✅ | zoom controls scale the scan past fit | 595 ms |
| ✅ | a coined tag carries to the next document, and Add works without Enter | 578 ms |
| ✅ | typing a tag and clicking away still commits it | 241 ms |

### `ng/panels.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | triage panel collapses to a rail and expands via nav-expand | 258 ms |
| ✅ | gold copy panel collapses independently via goldcopy-expand | 235 ms |

### `ng/queue-only.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | find tools collapse to leave just the queue; stack controls stay | 435 ms |
| ✅ | hiding the find tools returns the linguist to their stack | 516 ms |

### `ng/queue.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | content-type, language scope, and the stack ticks only when work is done | 629 ms |
| ✅ | queue progress ticks via strip decisions and the stack can run clear | 629 ms |
| ✅ | thread selection opens the viewer header | 186 ms |

### `ng/search.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | facet and group triage filters return scoped results | 1154 ms |
| ✅ | 4-language search: native scripts, cross-language, stats | 770 ms |
| ✅ | entity mode with a group implies selector search; hit opens the thread | 537 ms |

### `ng/viewer.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | thread queue loads and Arabic message renders RTL | 200 ms |
| ✅ | message search accepts native-script and cross-language queries | 879 ms |
| ✅ | search hit lands on the evidence with a flash | 2253 ms |
| ✅ | summary widget renders on demand and collapses out of the way | 265 ms |

### `ng/virtual-scroll.spec.js` — 5/5

| Result | Specification | Duration |
|---|---|---|
| ✅ | the message stream is windowed: a 2,000-message thread renders a bounded DOM | 265 ms |
| ✅ | scrolling a large thread stays inside the interaction budget | 378 ms |
| ✅ | sort order applies at volume and the toggle reverses it | 538 ms |
| ✅ | the reserved row height matches what a message actually renders at | 660 ms |
| ✅ | scrolling up slowly never leaves the window blank | 3111 ms |

### `services.spec.js` — 6/6

| Result | Specification | Duration |
|---|---|---|
| ✅ | search: thread list, thread messages, groups | 71 ms |
| ✅ | search: triage query returns matches, stats, and facet counts | 37 ms |
| ✅ | translate: fixture message translates to English | 46 ms |
| ✅ | entities: fixture message yields person + phone | 39 ms |
| ✅ | summarize: fixture thread yields summary | 48 ms |
| ✅ | ocr: fixture attachment yields full text and blocks | 47 ms |

### `shell.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | app shell renders the toolbar and UNCLASS banner | 138 ms |
| ✅ | dev proxy reaches the mock search service | 42 ms |

## React reference build

The evaluated Phase 2 implementation is retained in the repository unmodified and is
re-proven on every push. It is the restoration path if the Government prefers strict
parity with what it evaluated (see `hcd/one_output_model.md`).

| Result | Specification | Duration |
|---|---|---|
| ✅ | capture demo state | 1368 ms |
| ✅ | search: thread list, thread messages, groups | 214 ms |
| ✅ | search: triage query returns matches, stats, and facet counts | 37 ms |
| ✅ | translate: fixture message translates to English | 39 ms |
| ✅ | entities: fixture message yields person + phone | 33 ms |
| ✅ | summarize: fixture thread yields summary | 35 ms |
| ✅ | ocr: fixture attachment yields full text and blocks | 33 ms |
| ✅ | app shell renders the toolbar and UNCLASS banner | 392 ms |
| ✅ | dev proxy reaches the mock search service | 39 ms |
| ✅ | thread queue loads and Arabic message renders RTL | 505 ms |
| ✅ | translate, extract entities, and clip to gold copy with provenance | 567 ms |
| ✅ | message search accepts native-script and cross-language queries | 902 ms |
| ✅ | facet and group triage filters return scoped results | 1496 ms |
| ✅ | content-type, language scope, and queue disposition | 940 ms |
| ✅ | translation review and analyst note flow into gold copy | 746 ms |
| ✅ | officer document annotation: tag + note flow into search and gold copy | 675 ms |

## Other suites

| Suite | Command | Count |
|---|---|---|
| Angular unit (wire-format and shell) | `npm test --prefix angular` | 10 |
| Node unit (identity resolution, SigV4, certificate shapes) | `npm run test:node` | 59 |

## What is not covered

Stated rather than left to be discovered:

- **Screenshot-diff specs** were trimmed under the pre-agreed scope-trim order. Visual
  parity against the evaluated build is evidenced by side-by-side captures in
  `project/parity/` instead of by assertion.
- **No test runs against the target environment.** Every result here is from a local Kubernetes
  cluster in CI, which is the fallback the TDD states for pending sandbox access.
- **No test exercises the live model gateway.** `MODEL_ENDPOINT`, `MODEL_NAME` and
  `MODEL_API_KEY` are environment-specific and absent from the repository; the services
  answer from fixtures without them and log which mode they are in at startup.
- **The 200 ms interaction budget is measured on the corpus’s standing channel**, a
  2,388-message conversation collected over fourteen days and held as one thread
  because that is how a standing channel is worked (targeting direction, 8 Aug).
  `virtual-scroll.spec.js` additionally drives a synthetic 2,000-message thread as a
  controlled repeat. The budget itself is the TDD’s (Task 5).
