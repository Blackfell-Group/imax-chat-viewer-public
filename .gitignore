node_modules/
dist/
# Build output of deploy/make-offline-bundle.sh — a 55 MB image bundle plus the
# source archive. Rebuildable, and larger than the repository.
dist-bundle/
test-results/
playwright-report/
.gstack/
demo-state.png
*.log
.DS_Store

# never commit real keys or rasterized fixtures
.env
