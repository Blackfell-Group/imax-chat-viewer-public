# Test Results Report

**TDD data deliverable D3** · Agreement No. 5600002690012
River Hawk Consulting, LLC · UNCLASSIFIED

Generated by `scripts/make-test-report.js` directly from the Playwright JSON reporter
output in `project/evidence/`. The numbers below are read from the run, not transcribed,
so this document cannot claim a result the suite did not produce.

| Suite | Specs | Passed | Failed | Flaky | Duration | Run at | Verdict |
|---|---|---|---|---|---|---|---|
| Angular build (delivered) | 49 | 49 | 0 | 0 | 5.4 s | 2026-08-07 18:58:18Z | **PASS** |
| React reference (evaluated Phase 2 build) | 16 | 16 | 0 | 0 | 6.2 s | 2026-08-07 18:58:24Z | **PASS** |

Evidence files: `project/evidence/e2e-results-2026-08-07.json`, `project/evidence/react-reference-results-2026-08-07.json`.

## Angular build — specs by file

### `ng/bilingual.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | threads open bilingual: English leads, source always on screen, no toggle | 445 ms |
| ✅ | correction happens against the source; review state is obvious | 434 ms |

### `ng/customs-file.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | the customs file opens, pages through all five, and reads RTL | 625 ms |
| ✅ | the customs file carries an English gloss covering the whole document | 356 ms |
| ✅ | the customs file is reachable by its declaration number and its entities | 452 ms |

### `ng/enrichment.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | auto-translation renders and thread-level entity extraction chips every message | 479 ms |
| ✅ | translation review and analyst note (smoke-6 review flow, clips removed) | 441 ms |
| ✅ | the one output: export is locked until a thread is promoted, then carries verdicts and notes | 547 ms |

### `ng/flagged-facet.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | flagging surfaces a Flagged facet that isolates the handoff pile | 685 ms |
| ✅ | flagged-first sorting lifts the handoff pile without losing the rest | 269 ms |
| ✅ | the flagged filter clears with everything else | 446 ms |

### `ng/gold-copy-panel.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | copying the export confirms it happened | 671 ms |
| ✅ | the export dialog is large and maximizes | 622 ms |
| ✅ | gold threads reorder by drag, and the export follows the new order | 955 ms |
| ✅ | both side panels resize by dragging, and double-click restores | 324 ms |

### `ng/multipage-doc.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | multi-page Russian document: pages, Cyrillic blocks, and gloss | 639 ms |
| ✅ | the contract surfaces in a flagged-numbers watchlist sweep | 506 ms |

### `ng/ocr-threadgold.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | officer document annotation: tag + note, and the tag filters the queue | 618 ms |
| ✅ | OCR viewer: extracted blocks render with engine caption | 287 ms |
| ✅ | thread gold: translate thread, review all, promote — the stack ticks | 459 ms |

### `ng/ocr-viewer-size.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the scan viewer maximizes and the image grows with it | 659 ms |
| ✅ | zoom controls scale the scan past fit | 551 ms |
| ✅ | a coined tag carries to the next document, and Add works without Enter | 448 ms |
| ✅ | typing a tag and clicking away still commits it | 241 ms |

### `ng/panels.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | triage panel collapses to a rail and expands via nav-expand | 275 ms |
| ✅ | gold copy panel collapses independently via goldcopy-expand | 179 ms |

### `ng/queue-only.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | find tools collapse to leave just the queue; stack controls stay | 398 ms |
| ✅ | hiding the find tools returns the linguist to their stack | 487 ms |

### `ng/queue.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | content-type, language scope, and the stack ticks only when work is done | 628 ms |
| ✅ | queue progress ticks via strip decisions and the stack can run clear | 636 ms |
| ✅ | thread selection opens the viewer header | 149 ms |

### `ng/search.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | facet and group triage filters return scoped results | 1099 ms |
| ✅ | 4-language search: native scripts, cross-language, stats | 707 ms |
| ✅ | entity mode with a group implies selector search; hit opens the thread | 521 ms |

### `ng/viewer.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | thread queue loads and Arabic message renders RTL | 232 ms |
| ✅ | message search accepts native-script and cross-language queries | 776 ms |
| ✅ | search hit lands on the evidence with a flash | 2240 ms |
| ✅ | summary widget renders on demand and collapses out of the way | 267 ms |

### `ng/virtual-scroll.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | the message stream is windowed: a 2,000-message thread renders a bounded DOM | 275 ms |
| ✅ | scrolling a large thread stays inside the interaction budget | 387 ms |
| ✅ | sort order applies at volume and the toggle reverses it | 514 ms |

### `services.spec.js` — 6/6

| Result | Specification | Duration |
|---|---|---|
| ✅ | search: thread list, thread messages, groups | 85 ms |
| ✅ | search: triage query returns matches, stats, and facet counts | 49 ms |
| ✅ | translate: fixture message translates to English | 38 ms |
| ✅ | entities: fixture message yields person + phone | 48 ms |
| ✅ | summarize: fixture thread yields summary | 39 ms |
| ✅ | ocr: fixture attachment yields full text and blocks | 43 ms |

### `shell.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | app shell renders the toolbar and UNCLASS banner | 125 ms |
| ✅ | dev proxy reaches the mock search service | 32 ms |

## React reference build

The evaluated Phase 2 implementation is retained in the repository unmodified and is
re-proven on every push. It is the restoration path if the Government prefers strict
parity with what it evaluated (see `hcd/one_output_model.md`).

| Result | Specification | Duration |
|---|---|---|
| ✅ | capture demo state | 1287 ms |
| ✅ | search: thread list, thread messages, groups | 121 ms |
| ✅ | search: triage query returns matches, stats, and facet counts | 38 ms |
| ✅ | translate: fixture message translates to English | 40 ms |
| ✅ | entities: fixture message yields person + phone | 34 ms |
| ✅ | summarize: fixture thread yields summary | 38 ms |
| ✅ | ocr: fixture attachment yields full text and blocks | 34 ms |
| ✅ | app shell renders the toolbar and UNCLASS banner | 284 ms |
| ✅ | dev proxy reaches the mock search service | 40 ms |
| ✅ | thread queue loads and Arabic message renders RTL | 399 ms |
| ✅ | translate, extract entities, and clip to gold copy with provenance | 547 ms |
| ✅ | message search accepts native-script and cross-language queries | 914 ms |
| ✅ | facet and group triage filters return scoped results | 1512 ms |
| ✅ | content-type, language scope, and queue disposition | 935 ms |
| ✅ | translation review and analyst note flow into gold copy | 723 ms |
| ✅ | officer document annotation: tag + note flow into search and gold copy | 656 ms |

## Other suites

| Suite | Command | Count |
|---|---|---|
| Angular unit (wire-format and shell) | `npm test --prefix angular` | 10 |
| Node unit (identity resolution, SigV4, certificate shapes) | `npm run test:node` | 59 |

## What is not covered

Stated rather than left to be discovered:

- **Screenshot-diff specs** were trimmed under the pre-agreed scope-trim order. Visual
  parity against the evaluated build is evidenced by side-by-side captures in
  `project/parity/` instead of by assertion.
- **No test runs against the target environment.** Every result here is from a local Kubernetes
  cluster in CI, which is the fallback the TDD states for pending sandbox access.
- **No test exercises the live model gateway.** `MODEL_ENDPOINT`, `MODEL_NAME` and
  `MODEL_API_KEY` are environment-specific and absent from the repository; the services
  answer from fixtures without them and log which mode they are in at startup.
- **The 200 ms interaction budget is measured on a synthetic 2,000-message thread**,
  because the delivered corpus’s largest real thread is 18 messages. The budget itself
  is the TDD’s (Task 5); the synthetic thread is how it is made testable.
