# Test Results Report

**TDD data deliverable D3** · Agreement No. 5600002690012
River Hawk Consulting, LLC · UNCLASSIFIED

Generated by `scripts/make-test-report.js` directly from the Playwright JSON reporter
output in `project/evidence/`. The numbers below are read from the run, not transcribed,
so this document cannot claim a result the suite did not produce.

| Suite | Specs | Passed | Failed | Flaky | Duration | Run at | Verdict |
|---|---|---|---|---|---|---|---|
| Angular build (delivered) | 66 | 66 | 0 | 0 | 8.2 s | 2026-08-08 12:01:20Z | **PASS** |
| React reference (evaluated Phase 2 build) | 16 | 16 | 0 | 0 | 6.2 s | 2026-08-08 10:19:34Z | **PASS** |

Evidence files: `project/evidence/e2e-results-2026-08-08.json`, `project/evidence/react-reference-results-2026-08-08.json`.

## Angular build — specs by file

### `ng/bilingual.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | threads open bilingual: English leads, source always on screen, no toggle | 500 ms |
| ✅ | correction happens against the source; review state is obvious | 334 ms |

### `ng/bulk-confirm.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the bulk accept is not offered when reading each message is the job | 413 ms |
| ✅ | a long thread can be cleared in one action, after stating the count | 549 ms |
| ✅ | declining the accept changes nothing | 488 ms |
| ✅ | the gold copy reports bulk acceptance as what it was | 668 ms |

### `ng/conversation-note.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | a conversation note is recorded against the thread and shown above the stream | 560 ms |
| ✅ | the conversation note leads the gold copy, ahead of the transcript | 554 ms |
| ✅ | a conversation note can be withdrawn | 360 ms |
| ✅ | the note follows the conversation, not the screen | 434 ms |

### `ng/customs-file.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | the customs file opens, pages through all five, and reads RTL | 688 ms |
| ✅ | the customs file carries an English gloss covering the whole document | 378 ms |
| ✅ | the customs file is reachable by its declaration number and its entities | 466 ms |

### `ng/enrichment.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | auto-translation renders and thread-level entity extraction chips every message | 530 ms |
| ✅ | translation review and analyst note (smoke-6 review flow, clips removed) | 483 ms |
| ✅ | the one output: export is locked until a thread is promoted, then carries verdicts and notes | 576 ms |

### `ng/export-modal-fit.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | the transcript field stays inside the export dialog, restored and maximized | 725 ms |
| ✅ | maximize grows the dialog in both directions, not just sideways | 547 ms |

### `ng/flagged-facet.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | flagging surfaces a Flagged facet that isolates the handoff pile | 687 ms |
| ✅ | flagged-first sorting lifts the handoff pile without losing the rest | 304 ms |
| ✅ | the flagged filter clears with everything else | 453 ms |

### `ng/gold-copy-panel.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | copying the export confirms it happened | 791 ms |
| ✅ | the export dialog is large and maximizes | 743 ms |
| ✅ | gold threads reorder by drag, and the export follows the new order | 1211 ms |
| ✅ | both side panels resize by dragging, and double-click restores | 384 ms |

### `ng/multipage-doc.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | multi-page Russian document: pages, Cyrillic blocks, and gloss | 476 ms |
| ✅ | the contract surfaces in a flagged-numbers watchlist sweep | 488 ms |

### `ng/ocr-bilingual-lines.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | an RTL document puts the original on the right and the English on the left | 314 ms |
| ✅ | an LTR document puts the original on the left and the English on the right | 361 ms |
| ✅ | every line on the page is paired, and the pairing follows paging | 369 ms |

### `ng/ocr-gloss-review.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the document translation can be confirmed, and the verdict taken back | 449 ms |
| ✅ | the document translation can be corrected against the original | 342 ms |
| ✅ | the document can be sent back for a fresh translation | 499 ms |
| ✅ | the scan and the two text layers stack in reading order | 268 ms |

### `ng/ocr-threadgold.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | officer document annotation: tag + note, and the tag filters the queue | 464 ms |
| ✅ | OCR viewer: extracted blocks render with engine caption | 334 ms |
| ✅ | thread gold: translate thread, review all, promote — the stack ticks | 465 ms |

### `ng/ocr-viewer-size.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | the scan viewer maximizes and the image grows with it | 494 ms |
| ✅ | zoom controls scale the scan past fit | 580 ms |
| ✅ | a coined tag carries to the next document, and Add works without Enter | 476 ms |
| ✅ | typing a tag and clicking away still commits it | 234 ms |

### `ng/panels.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | triage panel collapses to a rail and expands via nav-expand | 258 ms |
| ✅ | gold copy panel collapses independently via goldcopy-expand | 243 ms |

### `ng/queue-only.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | find tools collapse to leave just the queue; stack controls stay | 417 ms |
| ✅ | hiding the find tools returns the linguist to their stack | 526 ms |

### `ng/queue.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | content-type, language scope, and the stack ticks only when work is done | 651 ms |
| ✅ | queue progress ticks via strip decisions and the stack can run clear | 683 ms |
| ✅ | thread selection opens the viewer header | 174 ms |

### `ng/search.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | facet and group triage filters return scoped results | 1168 ms |
| ✅ | 4-language search: native scripts, cross-language, stats | 735 ms |
| ✅ | entity mode with a group implies selector search; hit opens the thread | 508 ms |

### `ng/viewer.spec.js` — 4/4

| Result | Specification | Duration |
|---|---|---|
| ✅ | thread queue loads and Arabic message renders RTL | 264 ms |
| ✅ | message search accepts native-script and cross-language queries | 809 ms |
| ✅ | search hit lands on the evidence with a flash | 2268 ms |
| ✅ | summary widget renders on demand and collapses out of the way | 262 ms |

### `ng/virtual-scroll.spec.js` — 3/3

| Result | Specification | Duration |
|---|---|---|
| ✅ | the message stream is windowed: a 2,000-message thread renders a bounded DOM | 251 ms |
| ✅ | scrolling a large thread stays inside the interaction budget | 384 ms |
| ✅ | sort order applies at volume and the toggle reverses it | 527 ms |

### `services.spec.js` — 6/6

| Result | Specification | Duration |
|---|---|---|
| ✅ | search: thread list, thread messages, groups | 65 ms |
| ✅ | search: triage query returns matches, stats, and facet counts | 42 ms |
| ✅ | translate: fixture message translates to English | 40 ms |
| ✅ | entities: fixture message yields person + phone | 43 ms |
| ✅ | summarize: fixture thread yields summary | 40 ms |
| ✅ | ocr: fixture attachment yields full text and blocks | 44 ms |

### `shell.spec.js` — 2/2

| Result | Specification | Duration |
|---|---|---|
| ✅ | app shell renders the toolbar and UNCLASS banner | 138 ms |
| ✅ | dev proxy reaches the mock search service | 48 ms |

## React reference build

The evaluated Phase 2 implementation is retained in the repository unmodified and is
re-proven on every push. It is the restoration path if the Government prefers strict
parity with what it evaluated (see `hcd/one_output_model.md`).

| Result | Specification | Duration |
|---|---|---|
| ✅ | capture demo state | 1368 ms |
| ✅ | search: thread list, thread messages, groups | 214 ms |
| ✅ | search: triage query returns matches, stats, and facet counts | 37 ms |
| ✅ | translate: fixture message translates to English | 39 ms |
| ✅ | entities: fixture message yields person + phone | 33 ms |
| ✅ | summarize: fixture thread yields summary | 35 ms |
| ✅ | ocr: fixture attachment yields full text and blocks | 33 ms |
| ✅ | app shell renders the toolbar and UNCLASS banner | 392 ms |
| ✅ | dev proxy reaches the mock search service | 39 ms |
| ✅ | thread queue loads and Arabic message renders RTL | 505 ms |
| ✅ | translate, extract entities, and clip to gold copy with provenance | 567 ms |
| ✅ | message search accepts native-script and cross-language queries | 902 ms |
| ✅ | facet and group triage filters return scoped results | 1496 ms |
| ✅ | content-type, language scope, and queue disposition | 940 ms |
| ✅ | translation review and analyst note flow into gold copy | 746 ms |
| ✅ | officer document annotation: tag + note flow into search and gold copy | 675 ms |

## Other suites

| Suite | Command | Count |
|---|---|---|
| Angular unit (wire-format and shell) | `npm test --prefix angular` | 10 |
| Node unit (identity resolution, SigV4, certificate shapes) | `npm run test:node` | 59 |

## What is not covered

Stated rather than left to be discovered:

- **Screenshot-diff specs** were trimmed under the pre-agreed scope-trim order. Visual
  parity against the evaluated build is evidenced by side-by-side captures in
  `project/parity/` instead of by assertion.
- **No test runs against the target environment.** Every result here is from a local Kubernetes
  cluster in CI, which is the fallback the TDD states for pending sandbox access.
- **No test exercises the live model gateway.** `MODEL_ENDPOINT`, `MODEL_NAME` and
  `MODEL_API_KEY` are environment-specific and absent from the repository; the services
  answer from fixtures without them and log which mode they are in at startup.
- **The 200 ms interaction budget is measured on the corpus’s standing channel**, a
  2,388-message conversation collected over fourteen days and held as one thread
  because that is how a standing channel is worked (targeting direction, 8 Aug).
  `virtual-scroll.spec.js` additionally drives a synthetic 2,000-message thread as a
  controlled repeat. The budget itself is the TDD’s (Task 5).
