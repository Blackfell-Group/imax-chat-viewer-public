# Live model gateway. Unset => fixture-backed demo build (the default).
# BOTH values are environment-specific and supplied by the Sponsor for the
# target enclave — do not commit real values.
MODEL_ENDPOINT=
MODEL_API_KEY=
MODEL_NAME=gpt-4o
MODEL_TIMEOUT_MS=20000
